# Приложение для управления контактами

Spring Boot приложение для управления контактами.
Оно предоставляет функциональность для работы с контактами через репозитории и сервисы,
включая добавление, удаление, обновление и поиск контактов.

При первом запуске приложения автоматически создается профиль админа,
которому доступна своя панель управления, в том числе - генерация отчетов, доступ к Swagger и мониторинг Java Melody:

- Имя пользователя: ```admin```
- Пароль: ```admin```

## ER-диаграмма
![](er-diagram.png)

## Установка
1. Клонируйте репозиторий:
```
git clone https://github.com/cptntotoro/naumen_3.git
```

2. Соберите проект (без выполнения тестов):

```
mvn clean install -DskipTests
```

## 🚀 Как запускать

### В Docker

#### Только тесты

Исключены Selenium тесты.

```
docker-compose --profile test up --build --abort-on-container-exit
```

Если ответ 0, все тесты прошли успешно

#### Только продакшен

```
docker-compose --profile prod up -d
```

Приложение будет доступно по адресу: http://localhost:8080/

### Остановка контейнеров

Чтобы остановить все запущенные контейнеры, выполните:

```
docker-compose down
```

Если вы хотите также удалить тома (volumes), используйте:

```
docker-compose down -v
```

### Локально

#### Только тесты

1. Разверните БД согласно application-test.properties

2. Запустите тесты

```
mvn test
```

#### Только продакшен

1. Разверните БД согласно application.properties

2. Соберите проект:

```
mvn clean package -DskipTests
```

3. Запустите приложение:
```
java -jar target/NauJava-0.0.1-SNAPSHOT.jar
```

Приложение будет доступно по адресу:

```
http://localhost:8080/
```

## Мониторинг

Мониторинг (админ): http://localhost:8080/monitoring
Actuator: http://localhost:8080/actuator